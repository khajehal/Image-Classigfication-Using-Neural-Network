# Image-Classigfication-Using-Neural-Network

We will build two models:

* A two-layer neural network
* An L-layer neural network
 
You will in fact use these models to classify cat vs non-cat images!
